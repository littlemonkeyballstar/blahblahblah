/* Deprecated — site uses pre-built site.css. Kept for cached old HTML only. */
(function () {
  if (!document.querySelector('link[href="site.css"]')) {
    document.documentElement.classList.add('no-tailwind');
  }
})();